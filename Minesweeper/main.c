# include "minesweeper.h"
# include "dtekv_board.h"

/*int main() {
    // Initialize random seed
    srand_custom(123456789);  // Or use a more dynamic seed if available
    
    // Initialize VGA control
    init_vga();  // Instead of using global VGA_ctrl
    
    // Initialize game with default difficulty
    init_game(0);  // Start with easy difficulty
    
    // Draw initial board
    draw_current_board();  // Use the fixed function from previous suggestions
    
    // Start the game loop
    game_loop();
    
    return 0;
} */